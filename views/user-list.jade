extends layout

block body
    div.page-header
        h1 Users List

    if users.length
        div.list-group
            for user in users
                    a(class='list-group-item', href='/users/#{user.id}') #{user.name}
    else
        p There are no users currently.